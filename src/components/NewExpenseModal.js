import React from'react'
import { Modal } from 'react-bootstrap'

const NewExpenseModal = (props) => {
    return (
        <Modal>
            <Modal.Header closeButton>Enter new Expense</Modal.Header>
            <Modal.Body>
                
            </Modal.Body>
        </Modal>
    )
}

export default NewExpenseModal